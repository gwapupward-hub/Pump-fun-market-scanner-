# Example user crontab entry (UTC) — adjust paths to your install.
SHELL=/bin/bash
PATH=/usr/local/bin:/usr/bin:/bin

5 0 * * * cd /opt/pump-intel && set -a && source .env && set +a && pump-intel run-job >>/var/log/pump-intel.log 2>&1
